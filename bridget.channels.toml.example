# bridget.channels.toml — per-channel agent routing for bridget.
#
# Copy this file to ~/.pogo/bridget.channels.toml and edit the snowflakes /
# agent names to match your install. With this file present, bridget treats
# Discord channels as per-agent inboxes / outboxes — type in #ops to mail the
# `ops` agent, see `pm-product`'s digests in #pm-product, etc. Without it,
# bridget is DM-only and behaves identically to v1.0.0.
#
# To find a channel snowflake: in Discord, enable Developer Mode (User Settings
# → Advanced → Developer Mode), right-click the channel name, then "Copy
# Channel ID". Snowflakes are 18–20 digit strings; quote them as TOML strings
# so JSON-style integer-precision quirks don't trim digits.
#
# The bot must be a member of each channel's guild and have permission to read
# message history and send messages there. Required Discord intents (`guilds`,
# `guild_messages`) are non-privileged and bridget enables them automatically.
#
# Schema (per [channels.<name>] entry):
#   snowflake  optional  — Discord channel ID, as a quoted string of digits.
#                          OMIT it to have bridget auto-create the channel on
#                          startup (see "Auto-create" below).
#   agent      required  — Pogo agent name. Inbound free-form text is mailed
#                          to this agent; outbound events involving this agent
#                          fan out to the channel.
#   direction  optional  — "inbound" | "outbound" | "both". Default: "both".
#   kinds      optional  — Subset of ["mail", "task-transitions",
#                          "idea-claims"]. Filters which outbound classes
#                          fan out to this channel. Default: all three.
#   channel    optional  — Discord channel NAME to create / resolve-by-name
#                          when there is no snowflake. Default: the <name>
#                          label, lowercased to Discord's channel-name rules.
#
# The <name> in [channels.<name>] is a local label. When you omit `snowflake`
# it also seeds the created channel's name (unless `channel` overrides it), so
# pick something channel-shaped.
#
# ── Auto-create (no snowflake needed) ────────────────────────────────────────
# An entry with no `snowflake` is a channel bridget ensures exists: on startup
# it looks for a text channel with the target name, adopts it if found, and
# otherwise creates one (the bot needs the "Manage Channels" permission in the
# guild). The resulting channel ID is written to
# ~/.pogo/bridget.channel-ids.json so restarts resolve the same channel and
# never make a duplicate. That registry ID takes precedence over a `snowflake`
# you later hand-type for the same <name>. If a hand-typed snowflake can't be
# resolved, bridget resolves the entry by name (adopt-or-create) instead of
# retrying a dead id forever.
#
# WITHOUT the "Manage Channels" permission and with no pre-existing channel of
# that name to adopt, the entry never resolves. Its `mail` is then NOT relayed
# anywhere — not to a channel and not to a DM (see the routing note below) —
# and it is not lost either: it stays unread in ~/.macguffin/mail/<agent>/new/
# until the entry resolves. bridget says so in bridget.log once an hour, with
# the size of the backlog:
#
#   agent-mail: no channel wired for mayor — its mail is NOT being relayed to
#   any surface (no DM fallback on this path); 4121 mail waiting. ...
#
# That notice exists because this state held silently for 35 days on the
# author's host and swallowed 6,899 mail while startup printed
# `per-channel routing: on — 6 channel(s)` (mg-879c). Grant the permission, or
# hand-write a `snowflake` for an existing channel.

# ── Example 1: a two-way operator channel ────────────────────────────────────
# Type in #general → goes to mayor's mailbox. Mayor's outbound mail and task
# transitions fan out here instead of cluttering your DMs.
[channels.general]
snowflake = "111111111111111111"
agent     = "mayor"
direction = "both"

# ── Example 2: outbound-only digest channel ──────────────────────────────────
# pm-product's mail and idea-claims show up here, but typing in this channel
# does *nothing* — it's a read-only fan-out target. Use this shape when you
# want a notification feed without giving operators a way to reply.
[channels.pm-product-digest]
snowflake = "222222222222222222"
agent     = "pm-product"
direction = "outbound"
kinds     = ["mail", "idea-claims"]

# ── Example 3: inbound-only ops channel ──────────────────────────────────────
# Messages typed here go to the `ops` agent's mailbox, but ops's notifications
# stay in DMs (or wherever else they're routed). Useful when you want a team
# to be able to file work without subscribing them to ops's outbound chatter.
[channels.ops]
snowflake = "333333333333333333"
agent     = "ops"
direction = "inbound"

# ── Example 4: auto-created channel (no snowflake) ───────────────────────────
# No snowflake: bridget creates (or adopts) a text channel named `mayor-ops`
# on startup, wires it to the `mayor` agent both ways, and remembers its ID in
# ~/.pogo/bridget.channel-ids.json so it's stable across restarts. Add
# `channel = "some-other-name"` to control the created channel's name.
[channels.mayor-ops]
agent     = "mayor"
direction = "both"

# ── Notes on routing ─────────────────────────────────────────────────────────
# - Workflow verbs (`approve`, `reject`, `revise`, `explain`, `next mg-XXXX`,
#   `idea:`, `bug:`) keep routing through POGO_WORKFLOW_AGENT (default
#   `architect`) regardless of which channel you typed them in — design
#   coordination shouldn't change identity based on channel.
# - Free-form (non-verb) text in an inbound-mapped channel is mailed to the
#   channel's `agent` with the first line as the subject and the rest as the
#   body.
# - Outbound events for an agent with NO mapping (or whose mapping excludes the
#   event's `kind`) fall back to DMing DISCORD_USER_ID — exactly the v1.0.0
#   behavior.
# - That fallback covers task-transitions and idea-claims. It does NOT cover
#   `mail` for an agent that HAS a mapping which has not resolved to a live
#   channel: the per-agent mail watcher deliberately does not poll a mailbox it
#   has nowhere to put, because polling would mark the mail seen and lose it.
#   "Configured but unresolved" and "not configured" are therefore different
#   states with different outcomes; only the second DMs you.
# - Two entries pointing at the same snowflake with overlapping inbound
#   directions will collide; bridget keeps the first and logs the duplicate
#   to stderr.
